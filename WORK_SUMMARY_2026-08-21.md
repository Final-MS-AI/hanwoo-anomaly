# 2026년 8월 21일 작업 정리

## 1. 관리자 페이지 구축

- 한글 관리자 페이지 구현
- 관리자 메뉴 구성
  - 개요
  - 피드백함
  - 학습 루프
  - 사용자 관리
- COWOW 웹 스타일에 맞춘 관리자 UI 적용
- ID 역전파와 피드백 루프 중심의 화면 구성

## 2. 관리자 로그인 및 권한 인증

- `/admin` 전용 관리자 로그인 구현
- 일반 `/login` 페이지에는 관리자 로그인 UI가 표시되지 않도록 분리
- 관리자 계정 로그인 후 `/admin`으로 이동하도록 수정
- 관리자 권한이 없는 사용자의 관리자 페이지 접근 차단
- 관리자 권한을 DB의 `admin_users` 테이블로 관리
- 테스트 관리자 계정 적용
  - 아이디: `admin`
  - 비밀번호: `1234`

## 3. ID 역전파 및 muzzle API 연결

실제 muzzle API와 관리자 페이지를 연결했습니다.

- 트랙 목록 조회
- 트랙 상세 정보 조회
- ID 바인딩
- 바인딩 해제
- 현재 바인딩 상태 확인
- 과거 관측 프레임 수 확인

### 상세 화면 동작

- 목록에서는 모든 항목을 `상세`로 표시
- `상세` 버튼을 누르면 오른쪽 상세 패널 표시
- 미확정 트랙은 상세 패널 안에서 바로 바인딩 가능
- segment ID는 자동 입력
- 가축이력번호와 유사도 입력 후 `ID 바인딩 적용`
- 바인딩 성공 후 트랙 목록과 상세 상태 자동 갱신

## 4. 관리자 알림 시스템

### DB 및 API

- `admin_notifications` 테이블 추가
- 관리자 알림 목록 조회 API 구현
- 개별 알림 읽음 처리
- 전체 알림 읽음 처리

### 알림 발생 이벤트

- ID 역전파 바인딩 완료
- 트랙 바인딩 해제
- 관리자 권한 추가
- 관리자 권한 해제

### 화면 반영

- 우측 상단 알림 버튼 구현
- 읽지 않은 알림 개수 표시
- 알림 목록 패널 구현
- 최근 활동을 목업이 아닌 실제 알림 데이터 기반으로 표시
- 알림이 없을 때 빈 상태 표시

## 5. UI 개선

- 상세 정보 패널을 하단 영역에서 오른쪽 오버레이 패널로 변경
- 상세 패널 닫기 버튼 추가
- 승인 및 보류 버튼의 체크·X 표시 위치 수정
- 피드백함의 고정 배지 숫자 `12` 제거
- 실제 검토 대기 트랙 수를 동적으로 표시
- 고정된 목업 최근 활동 제거
- 각종 알림과 상태 메시지가 일정 시간 후 자동으로 사라지도록 처리

## 6. HTTPS 및 Azure 배포

- 관리자 API가 정적 프론트엔드 fallback으로 처리되지 않도록 Caddy 라우팅 수정
- `/admin/*` 관리자 API를 백엔드로 전달
- HTTP 접속 시 HTTPS로 자동 전환
- Azure 서버에 프론트엔드 최신 빌드 배포
- 정적 파일 권한 문제로 발생한 403 오류 해결

## 7. 로딩 성능 개선

서버 API 응답이 느린 상황에서도 화면이 즉시 보이도록 개선했습니다.

- 최근 트랙 목록을 브라우저 세션에 캐시
- 최근 알림 목록을 브라우저 세션에 캐시
- 상세 조회 결과를 메모리에 캐시
- 캐시된 데이터는 먼저 표시하고 최신 데이터는 백그라운드에서 갱신
- 새로고침 중 기존 목록이 사라지지 않도록 수정
- 알림 패널을 닫을 때 불필요한 재조회 방지

## 8. 검증 결과

- 프론트엔드 프로덕션 빌드 성공
- 실제 muzzle 트랙 목록 API 조회 성공
- 실제 트랙 상세 API 조회 성공
- 관리자 알림 API 조회 성공
- 미인증 관리자 API가 `401`로 차단되는 것 확인
- Azure 관리자 페이지 응답 `200` 확인
- 최신 정적 JavaScript 파일 응답 `200` 확인

실제 운영 데이터가 변경될 수 있으므로 테스트 과정에서 임의의 트랙을 실제 바인딩하는 작업은 수행하지 않았습니다.

## 9. 남은 작업

- 운영용 관리자 비밀번호 변경
- 실제 테스트용 트랙을 정한 뒤 바인딩 성공 흐름 최종 확인
- 최신 변경사항 GitHub 커밋 및 푸시
- 운영 환경에서 관리자 페이지 전체 화면 최종 점검

## 참고 파일

- `front/src/AdminPage.jsx`
- `front/src/AdminPage.css`
- `backend/admin_auth.py`
- `backend/admin_login.py`
- `backend/admin_notifications.py`
- `backend/admin_notification_store.py`
- `muzzle/api/admin_auth.py`
- `muzzle/api/tracks_router.py`
- `db/migrations/20260821_create_admin_users.sql`
- `db/migrations/20260821_create_admin_notifications.sql`
