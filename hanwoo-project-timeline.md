
---

## 2026-08-12 진행 (개체 식별 파트)

**ID 역전파 종료.** 8/6부터 다섯 차례 1순위로 이월된 블로커가 해소됐다.

| 항목 | 상태 |
|---|---|
| 영상 → 식별 → 자동 바인딩 (`auto_bind.py`) | ✅ |
| `GET /muzzle/cattle/{national_id}/timeline` | ✅ 배포 |
| 테스트 데이터 격리 (`session_id` 접두어) | ✅ |
| 이상행동 파트 통합 지점 확정 | ✅ |
| 행동 데이터 DB 적재 (트랙 21건) | ✅ |
| 카메라 간 시각 정합 (`--start-time`) | ✅ |
| 카메라 A↔B 핸드오프 본체 | ⏸ |

**검증 근거:** segment 6 에 바인딩 1행 INSERT → `affected_observations: 19`.
코가 보인 것은 프레임 52~62 인데 `frame_idx 0` 이 `similarity 0.8564` 를 획득했다.
식별 시점 이전 관측에 개체번호가 소급 적용된 증거다.

**이상행동 파트와의 접합 구조 (확정)**

GPU VM 에서 `behavior_extract.py` 가 한우 검출 + ByteTrack + 행동 분류를 돌려
JSONL 을 만들고, DB VM 의 `track_load.py` 가 이를 `track_observation` 에 적재한다.
이후 `auto_bind.py` 로 개체번호를 붙이면 `timeline` 으로 조회된다.
추론과 적재를 분리했으므로 **GPU VM 에 DB 자격증명이 필요 없다.**

검출·추적·행동은 이상행동 파트의 `hanwoo_detector_v5_best.pt` 를 쓴다. 같은 종류
영상에서 트랙 21건이 잡혔다 — 범용 `yolov8s`(COCO 소) 는 1건이었다.

**8/16 E2E 잔여**

| # | 작업 | 소요 |
|---|---|---|
| 1 | `timeline` 응답에 `behavior` 노출 | 15분 |
| 2 | 모델 가중치 백업 (8/10부터 3회 이월) | 30분 |
| 3 | 행동 트랙에 개체번호 바인딩 | 1시간 |
| 4 | 규칙 판정 엔드포인트 (`prolonged_lying` 등) | 2시간 |
| 5 | 카메라 A↔B 핸드오프 본체 | 1.5시간 |

**핸드오프 설계 변경:** 전경 카메라가 초크포인트까지 촬영한다는 점이 확인돼
구현 가능성이 열렸다. 구현 시 화면 좌표를 쓰지 않고 시간 정합(±3초, 후보 2건
이상이면 보류)으로 간다 — 영상이 바뀌어도 다시 설정할 것이 없다.


---

## 2026-08-14 진행 (개체 식별 파트)

**8/16 E2E 요건을 이틀 앞당겨 충족했다.** 영상 두 편에서 timeline 까지 한 명령
으로 관통한다. 소요 1분 46초.

| 항목 | 상태 |
|---|---|
| `timeline` 응답에 `behavior` 노출 | 완료 |
| 행동 트랙에 개체번호 바인딩 (배관 검증) | 완료 |
| E2E 오케스트레이터 (`run_e2e.py`) | 완료 |
| 카메라 A↔B 핸드오프 본체 | 완료 |
| 모델 가중치 백업 | 확인 종료 — 이미 완료 상태였다 |
| 규칙 판정 엔드포인트 | 미착수 — 다음 1순위 |

**관통 경로**

    run_e2e.py --wide <전경> --choke <초크포인트>
      GPU VM 검출·추적·행동분류 -> JSONL -> 회수 -> 적재
      -> 비문 식별 -> 시간+구역 정합 -> 바인딩 -> timeline

**핸드오프 설계 변경 — 시간 정합만으로는 안 된다**

8/12 에 정한 규칙(T±3초에 살아있던 트랙이 1건이면 바인딩)을 구현했더니 후보가
23트랙 중 10건 남았다. 창을 3초에서 1초로 줄여도 1건만 줄었다. 축사에 소가 상시
여러 마리 있으면 "그때 살아 있던 트랙"이 곧 "전부"이므로 파라미터로 해결되지
않는다.

**초크포인트 구역을 사용자가 지정하는 방식으로 전환했다.** 그 시각 구역 안에
있던 트랙만 후보가 된다. 후보 10건이 1건으로 좁혀졌다.

중간에 행동 라벨(`feeding`)로 좁히는 안을 거쳐갔으나 폐기했다. 그 라벨은 급이대
부근에서 고개를 숙이면 붙는 것으로 확인돼, **화면 좌표를 쓰지 않는다는 주장이
성립하지 않았다.** 좌표 의존성이 사라진 것이 아니라 남의 모델 안에 들어가
통제권만 잃은 상태였다. 구역을 직접 받으면 농장마다 다른 급이대 위치에 대응되고
모델이 갱신돼도 흔들리지 않는다.

**판정 기준점은 `topright` 다.** 소는 몸을 급이대에 넣지 않고 고개만 넣으므로
bbox 아래(발)·중앙(몸통)은 구역 밖이다 — 두 기준 모두 후보 0건이었다. 6가지
기준점을 전부 계산해 실측으로 정했다. 카메라 배치가 다르면 `--anchor` 로 바꾼다.

**측정된 적용 범위:** 창 ±2초 기준 전 구간의 26% 에서 후보가 1건으로 좁혀지고
나머지는 보류된다. 둘 이상이 동시에 급이대에 있으면 구분되지 않는다 — 알려진
한계로 공개한다.

**8/16 잔여**

| # | 작업 | 소요 |
|---|---|---|
| 1 | 규칙 판정 엔드포인트 (`prolonged_lying` 등) | 2시간 |
| 2 | 시연 영상 확정 후 구역 재지정 | 30분 |
| 3 | GPU VM 병렬 비문 파이프라인과의 관계 정리 | 30분 |

8/12 에 1순위였던 `--stride 1` 재적재는 해소됐다. 379프레임 연속 트랙이 확보돼
`prolonged_lying` 의 전제 조건이 충족됐다.
