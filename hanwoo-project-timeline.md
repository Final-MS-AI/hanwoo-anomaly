
---

## 2026-08-12 진행 (개체 식별 파트)

**ID 역전파 종료.** 8/6부터 다섯 차례 1순위로 이월된 블로커가 해소됐다.

| 항목 | 상태 |
|---|---|
| 영상 → 식별 → 자동 바인딩 (`auto_bind.py`) | ✅ |
| `GET /muzzle/cattle/{national_id}/timeline` | ✅ 배포 |
| 테스트 데이터 격리 (`session_id` 접두어) | ✅ |
| 이상행동 파트 통합 지점 확정 | ✅ |
| 행동 데이터 DB 적재 (트랙 21건) | ✅ |
| 카메라 간 시각 정합 (`--start-time`) | ✅ |
| 카메라 A↔B 핸드오프 본체 | ⏸ |

**검증 근거:** segment 6 에 바인딩 1행 INSERT → `affected_observations: 19`.
코가 보인 것은 프레임 52~62 인데 `frame_idx 0` 이 `similarity 0.8564` 를 획득했다.
식별 시점 이전 관측에 개체번호가 소급 적용된 증거다.

**이상행동 파트와의 접합 구조 (확정)**

GPU VM 에서 `behavior_extract.py` 가 한우 검출 + ByteTrack + 행동 분류를 돌려
JSONL 을 만들고, DB VM 의 `track_load.py` 가 이를 `track_observation` 에 적재한다.
이후 `auto_bind.py` 로 개체번호를 붙이면 `timeline` 으로 조회된다.
추론과 적재를 분리했으므로 **GPU VM 에 DB 자격증명이 필요 없다.**

검출·추적·행동은 이상행동 파트의 `hanwoo_detector_v5_best.pt` 를 쓴다. 같은 종류
영상에서 트랙 21건이 잡혔다 — 범용 `yolov8s`(COCO 소) 는 1건이었다.

**8/16 E2E 잔여**

| # | 작업 | 소요 |
|---|---|---|
| 1 | `timeline` 응답에 `behavior` 노출 | 15분 |
| 2 | 모델 가중치 백업 (8/10부터 3회 이월) | 30분 |
| 3 | 행동 트랙에 개체번호 바인딩 | 1시간 |
| 4 | 규칙 판정 엔드포인트 (`prolonged_lying` 등) | 2시간 |
| 5 | 카메라 A↔B 핸드오프 본체 | 1.5시간 |

**핸드오프 설계 변경:** 전경 카메라가 초크포인트까지 촬영한다는 점이 확인돼
구현 가능성이 열렸다. 구현 시 화면 좌표를 쓰지 않고 시간 정합(±3초, 후보 2건
이상이면 보류)으로 간다 — 영상이 바뀌어도 다시 설정할 것이 없다.


---

## 2026-08-14 진행 (개체 식별 파트)

**8/16 E2E 요건을 이틀 앞당겨 충족했다.** 영상 두 편에서 timeline 까지 한 명령
으로 관통한다. 소요 1분 46초.

| 항목 | 상태 |
|---|---|
| `timeline` 응답에 `behavior` 노출 | 완료 |
| 행동 트랙에 개체번호 바인딩 (배관 검증) | 완료 |
| E2E 오케스트레이터 (`run_e2e.py`) | 완료 |
| 카메라 A↔B 핸드오프 본체 | 완료 |
| 모델 가중치 백업 | 확인 종료 — 이미 완료 상태였다 |
| 규칙 판정 엔드포인트 | 미착수 — 다음 1순위 |

**관통 경로**

    run_e2e.py --wide <전경> --choke <초크포인트>
      GPU VM 검출·추적·행동분류 -> JSONL -> 회수 -> 적재
      -> 비문 식별 -> 시간+구역 정합 -> 바인딩 -> timeline

**핸드오프 설계 변경 — 시간 정합만으로는 안 된다**

8/12 에 정한 규칙(T±3초에 살아있던 트랙이 1건이면 바인딩)을 구현했더니 후보가
23트랙 중 10건 남았다. 창을 3초에서 1초로 줄여도 1건만 줄었다. 축사에 소가 상시
여러 마리 있으면 "그때 살아 있던 트랙"이 곧 "전부"이므로 파라미터로 해결되지
않는다.

**초크포인트 구역을 사용자가 지정하는 방식으로 전환했다.** 그 시각 구역 안에
있던 트랙만 후보가 된다. 후보 10건이 1건으로 좁혀졌다.

중간에 행동 라벨(`feeding`)로 좁히는 안을 거쳐갔으나 폐기했다. 그 라벨은 급이대
부근에서 고개를 숙이면 붙는 것으로 확인돼, **화면 좌표를 쓰지 않는다는 주장이
성립하지 않았다.** 좌표 의존성이 사라진 것이 아니라 남의 모델 안에 들어가
통제권만 잃은 상태였다. 구역을 직접 받으면 농장마다 다른 급이대 위치에 대응되고
모델이 갱신돼도 흔들리지 않는다.

**판정 기준점은 `topright` 다.** 소는 몸을 급이대에 넣지 않고 고개만 넣으므로
bbox 아래(발)·중앙(몸통)은 구역 밖이다 — 두 기준 모두 후보 0건이었다. 6가지
기준점을 전부 계산해 실측으로 정했다. 카메라 배치가 다르면 `--anchor` 로 바꾼다.

**측정된 적용 범위:** 창 ±2초 기준 전 구간의 26% 에서 후보가 1건으로 좁혀지고
나머지는 보류된다. 둘 이상이 동시에 급이대에 있으면 구분되지 않는다 — 알려진
한계로 공개한다.

**8/16 잔여**

| # | 작업 | 소요 |
|---|---|---|
| 1 | 규칙 판정 엔드포인트 (`prolonged_lying` 등) | 2시간 |
| 2 | 시연 영상 확정 후 구역 재지정 | 30분 |
| 3 | GPU VM 병렬 비문 파이프라인과의 관계 정리 | 30분 |

8/12 에 1순위였던 `--stride 1` 재적재는 해소됐다. 379프레임 연속 트랙이 확보돼
`prolonged_lying` 의 전제 조건이 충족됐다.

---

## 2026-08-17 진행 (개체 식별 파트)

**역전파를 화면으로 꺼내고, 구역 지정을 제품 기능으로 전환했다.** PR 1건 머지.

| 항목 | 상태 |
|---|---|
| ID 역전파 잔여 작업 판정 | 로직은 8/14 종료 확인. 미비한 것은 화면이었다 |
| 역전파 결과 조회 페이지 | 완료 — 배포·검증 |
| `track_zone` 테이블 + 구역 CRUD API | 완료 |
| `zones.py` DB 우선 조회 + 폴백 | 완료 — `run_e2e.py` 무수정 |
| 구역 지정 화면 | 완료 — 배포·검증 |
| 온보딩 흐름에 구역 단계 편입 | 미착수 — React 빌드 봉쇄 |

**검증 근거:** `GET /muzzle/cattle/000000000010/timeline` 이 `segment 138`,
`observation_count 379` 를 반환하고 `frame_idx 0` 이 `similarity 0.8564` 를
갖는다. 코가 보인 것은 그보다 한참 뒤다 — 역전파가 살아 있다는 증거가 DB 에
보존돼 있었다.

**구역 하드코딩 해소.** 8/14 에 `--zone top` 으로 CLI 에 넣던 값을 사용자가
CCTV 화면에서 지정하게 바꿨다. 농장이 바뀌어도 코드를 고치지 않는다.
기존 `top` 구역은 무손실 이관했다 (꼭짓점 10개, DB/폴백 좌표 일치 True).

**설계 판단 — 팀원 테이블에 FK 를 걸지 않았다.** `cowow_devices.device_id` 와
대응하나 NULL 허용 컬럼으로 자리만 뒀다. 기기 스키마 확정 전이라 지금 FK 를
걸면 그쪽 변경이 이쪽을 깨뜨린다. **CREATE 만 했고 ALTER·DROP 은 없다.**

**React 경로는 봉쇄 상태다.** 공용 클론에 팀원 미커밋 15개가 있어 빌드하면
미완성 작업이 프로덕션에 나간다. 두 화면을 독립 정적 페이지로 만든 이유다.
`try_files {path} /index.html` 규칙상 실재 파일은 SPA 로 넘어가지 않는다.

## 2026-08-18 진행 (개체 식별 파트)

**한 일 — 역전파 검증 수단을 만들고, 그것이 데이터 손상을 잡았다.**

역전파의 잔여 작업을 판정하다가 **본체가 아니라 검증 수단이 없다는 것**을
확정했다. 확인 방법이 `timeline` 응답을 눈으로 읽는 것뿐이라 조용히 깨지는
경로를 탐지할 수 없었다. 불변식 6개를 검사하는 도구를 만들었고, 만든 첫날
**중복 (세그먼트, 프레임) 쌍 2,832건**을 탐지했다.

원인은 8/14 에 이미 문서화해 둔 현상이었다(`muzzle-api-notes.md` 17.3).
같은 세션으로 두 번 적재하면 관측만 두 배가 된다. **규칙은 적었고 강제 수단이
없었으며, 재현해 둔 데이터도 정리하지 않아 4일간 오염으로 남았다.**

**왜 육안으로 못 잡았나.** `track_segment` 에는 UNIQUE 가 있어 세그먼트는
중복되지 않고 `frame_count` 도 정상값을 유지한다. 관측 행수를 고유 프레임
수와 비교해야만 드러난다. 그리고 중복은 `affected_observations` 를 **키우는**
방향으로 깨지므로, 모르고 보면 오히려 더 잘 된 것처럼 보인다.

**수습.** 백업 → 예상 건수 대조 → 삭제 → 재검증 → 잔존 확인 순서로 처리하고,
어느 단계든 건수가 어긋나면 자동 롤백하게 했다. 5,664 -> 2,832 행.
**발표 대상 `seg 138` 은 379행 무손상이다.** 중복이 수치를 키우는 방향이라
이 확인이 진단의 가장 중요한 목적이었다.

**재발 방지는 두 층 모두.** DB 에 유일 인덱스, 코드에 `ON CONFLICT DO NOTHING`.
한 층만 고치면 중복이 다시 들어오거나 재실행이 예외로 죽는다. 5프레임 JSONL 을
두 번 적재해 **`5 -> 5`, 종료코드 0** 으로 멱등성을 실측 검증했다.

**작업 범위 판정 — 3회 이월된 항목 두 개를 내려놓았다.** 규칙 판정
엔드포인트는 8/12 워크로그에 내가 직접 "이상행동 파트 최종 산출물"이라고
써놓고도 잔여 목록에 세 차례 유지했다. 소가 **누구**인지가 내 파트이고, 그
행동이 **이상한지**는 이상행동 파트다. 내 계약상 산출물인 "개체별 시계열
제공"은 8/14 에 이미 끝났다. `segments` 행동 집계도 같은 이유로 제외했다 —
행동 정보는 이미 `observations` 로 전량 노출돼 있어 계약 누락이 아니다.

**CI 체크 실패는 코드 문제가 아니었다.** PR #37 의 `Build and Deploy Job` 이
실패했으나 Oryx 빌드는 성공했고, Azure Static Web Apps 스테이징 환경 한도
때문에 마지막 업로드만 거부됐다. 변경 파일에 프론트가 0개이고 `main` push
프로덕션 배포는 20건 연속 성공이었다. 머지하면 `Close Pull Request Job` 이
프리뷰를 지워 슬롯이 회수된다 — 실제로 그렇게 풀렸다.

**남긴 것.** `paths` 필터로 PR 프리뷰 생성을 프론트 변경 시에만 하도록 하는
근본안은 제안만 하고 실행하지 않았다. 프론트 배포 경로라 잘못 건드리면 팀
전체 배포가 멈춘다.

| 확정 수치 | 값 |
|---|---|
| 삭제한 중복 관측 행 | 2,832 (5,664 -> 2,832) |
| 관측 행 총계 | 14,714 -> 11,882 |
| 오염 세션 | 1 / 7 (세그먼트 62~84) |
| **`seg 138` (발표 대상)** | **379행 무손상** |
| 역전파 확보 관측 비율 | 2.6% -> **3.2%** (분모 정정) |
| 소급 증거 세그먼트 | 3건 (seg 6·10·138, 모두 frame 0 부터) |
| 불변식 검사 | 5/6 FAIL -> **6/6 PASS** (종료코드 0) |
| 재적재 멱등성 | **5 -> 5** |

PR #37 머지 완료 (`c2e530a`). 프로덕션 배포 성공.

**8/19 이후 잔여**

| # | 작업 | 소요 | 비고 |
|---|---|---|---|
| 1 | 백엔드 파트에 기기 스키마 질의 (`device_id` FK 여부) | 10분 | |
| 2 | 팀에 Azure SWA 슬롯 한도 공유 | 10분 | 열린 모든 PR 에 영향 |
| 3 | `track_observation_bak_20260817` DROP | 5분 | 며칠 후 |
| 4 | 역전파 적용 범위 발표 자료 반영 | 1시간 | 관측 3.2%, 핸드오프 26% |
| 5 | 교차 도메인 한계 발표 자료 반영 | 1시간 | |
| 6 | 온보딩 흐름에 구역 단계 편입 | 2시간 | 팀원 커밋 대기 |
| 7 | 대시보드 개체 상세에 식별 이력 탭 | 2시간 | 팀원 커밋 대기 |

**규칙 판정 엔드포인트는 잔여에서 제외했다** — 이상행동 파트 산출물이다.
인계 시 전달할 사실: JSONL 에 `"behavior": "lying"` 이 실재하며 379프레임
연속 트랙이 확보돼 있으므로 `prolonged_lying` 의 전제는 충족돼 있다.

## 2026-08-18~19 진행 (개체 식별 파트)

**구역 지정 화면을 제품 안으로 완전히 들여왔다.** 진입점 추가(#39) -> 영상 분석
탭으로 이동(#40) -> 소 등록 카드 디자인 적용 및 탭 라벨 교체(#41) -> iframe
임베드(#44) -> 창 속의 창·하단 여백 정리(#43, #45) 순으로 팀 피드백을 반영했다.
#42 는 충돌로 폐기.

| 항목 | 상태 |
|---|---|
| 하단 탭 "구역 지정" (경로 `/inference` 유지) | 완료 |
| 페이지 이동 없이 앱 내 표시 (iframe) | 완료 |
| 소 등록 카드와 동일한 디자인 | 완료 |
| 다개체 행동 탐지 카드 대체 | 완료 (팀 요청) |
| 정적 페이지 별도 루트 분리 | 완료 (`/var/www/hanwoo-static/`) |
| VM 배포 및 실물 확인 | 완료 |
| 팀원 웹뷰 착수 가능 | 완료 |

**진입점이 없으면 앱에서는 존재하지 않는 화면이다.** 팀원 앱은 WebView 라
주소창이 없다. 최종적으로 iframe 임베드를 골랐는데, 이유는 **검증 비용**이다.
캔버스를 React 로 포팅하면 8/17 에 실측한 다각형·422 검사·기준점 로직을 전부
재증명해야 한다.

**★ `curl` 상태 코드 200 이 거짓이었다.** 정적 페이지 두 장이 `rsync --delete` 로
삭제됐는데 SPA 폴백이 495 bytes 껍데기를 내보내 200 이 유지됐다. 하루 동안
소실을 탐지하지 못했다. 별도 루트로 분리해 항구 해소했다.

**★ 셸 `&&` 체인이 거짓 성공을 만들었다.** `npm run build | head` 는 빌드 실패를
통과시켜 깨진 코드가 PR 까지 올라갔고, `grep -c 0` 은 체인을 끊어 이전 로그를
읽은 "성공" 을 출력했다. 검증 명령은 한 줄씩 독립 실행한다.

**팀원 재배포가 내 배포를 덮는다.** 배포 전 공유 합의가 필요하다.